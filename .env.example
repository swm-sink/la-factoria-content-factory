# La Factoria Environment Configuration
# Only API keys are required - everything else has smart defaults!

# Required: AI Service Keys
GEMINI_API_KEY=your-gemini-api-key-here
ELEVENLABS_API_KEY=your-elevenlabs-api-key-here

# That's it! The system will automatically configure:
# ✓ Database URL (PostgreSQL)
# ✓ Redis URL
# ✓ Port (8000 or Railway PORT)
# ✓ Environment detection
# ✓ CORS origins
# ✓ Storage (local in dev, S3/R2 in production)
# ✓ Caching strategies
# ✓ Rate limits
# ✓ Security settings

# Optional: Override defaults if needed
# ENV=production
# DATABASE_URL=postgresql://custom-db-url
# REDIS_URL=redis://custom-redis-url
# PORT=3000
# JWT_SECRET=your-production-secret
# SENTRY_DSN=your-sentry-dsn
# LLM_TIMEOUT_SECONDS=120  # Timeout for AI API calls (default: 120s)